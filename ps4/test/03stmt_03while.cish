main() { 
  let i = 0;
  let x = 0; {
    printInt(i);
    printInt(x);
    printInt(i < 10);
    while (i < 10) {
      x = x + i;
      i = i + 1;
      printInt(x);
      printInt(i);
    }
    return x;
  }
}

