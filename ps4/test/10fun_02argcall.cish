f(a) {
  printInt(a);
  return a * a;
}
main() {
  let sum = 0;
  let i = 0; {
    for (i = 1; i <= 10; i = i + 1) {
      printInt(i);
      sum = sum + f(i);
    }
    printInt(sum);
    return sum;
  }
}
